[认证信息]
# 从火山引擎控制台获取的 APP ID
app_id = 123456789

# 从火山引擎控制台获取的 Access Token
access_key = your-access-key

[默认设置]
# 默认模式：s2s（语音到语音）或 s2t（语音到文字）
default_mode = s2s

# 默认源语言：zh（中文）或 en（英文）
default_source_language = zh

# 默认目标语言：zh（中文）或 en（英文）
default_target_language = en

